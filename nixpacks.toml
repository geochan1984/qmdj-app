[phases.build]
cmds = [
  "pip install -r requirements.txt",
  "python manage.py collectstatic --noinput",
  "python manage.py migrate --noinput",
  "python init_db.py"
]

[start]
cmd = "gunicorn qmdj_project.wsgi --bind 0.0.0.0:$PORT --workers 2 --timeout 120"
